import * as React from 'react';

interface ICustomSliderProps {
    className?: string;
}

export const CustomSlider: React.FC<ICustomSliderProps> = ({className}) => {
    return (
        <>
        </>
  ) ;
};


